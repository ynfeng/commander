project.dependencies {
    implementation(
            project(':server'),
            project(':cluster'),
            project(':support'),
            project(':cluster-atomix-provider')
    )
    testImplementation(
            project(':testutil')
    )
}
