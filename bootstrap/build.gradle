project.dependencies {
    implementation(
            project(':server'),
            project(':cluster'),
            project(':cluster-atomix-provider')
    )
    testImplementation(
            project(':testutil')
    )
}
