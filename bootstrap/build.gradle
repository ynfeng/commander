project.dependencies {
    implementation(
            project(':server'),
            project(':cluster'),
            project(':support')
    )
    testImplementation(
            project(':testutil')
    )
}
